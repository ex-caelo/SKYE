{
  "name": "@skye/app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:views:browser": "playwright test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@azure/msal-browser": "^5.21.0",
    "@microsoft/microsoft-graph-client": "^3.0.7",
    "@skye/form-config": "workspace:*",
    "astro": "^7.3.1",
    "invokers-polyfill": "^1.0.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
