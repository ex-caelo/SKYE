{
  "name": "@skye/form-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:configs": "tsx ./scripts/lintConfigs.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "ajv": "^8.20.0"
  },
  "devDependencies": {
    "vitest": "^5.0.0",
    "typescript": "^7.0.2",
    "tsx": "^4.23.13"
  }
}
